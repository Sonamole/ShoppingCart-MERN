// Exporting an object containing collection names
module.exports = {

    PRODUCT_COLLECTION: 'product',// Name of the collection(table) that stores product information
    USER_COLLECTION: 'user', // Name of the collection(table)  that stores user information
    CART_COLLECTION: 'cart', // Name of the collection(table)  that stores shopping cart information
    ORDER_COLLECTION:'order',
};
