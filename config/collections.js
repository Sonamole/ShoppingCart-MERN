// Exporting an object containing collection names
module.exports = {
    // Name of the collection(table) that stores product information
    PRODUCT_COLLECTION: 'product',

    // Name of the collection(table)  that stores user information
    USER_COLLECTION: 'user',

    // Name of the collection(table)  that stores shopping cart information
    CART_COLLECTION: 'cart',
};
